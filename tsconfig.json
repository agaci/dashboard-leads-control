{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    // Permite `import './zonas.ts'` com a extensao. Sem isto, um modulo puro nao pode
    // importar outro modulo puro sem deixar de ser testavel: o `npm test` corre com o
    // strip-types do Node, que exige a extensao explicita. So e legal com `noEmit`.
    "allowImportingTsExtensions": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": [
    "node_modules",
    // Testes corridos por `npm test` com o strip-types do Node, que exige a
    // extensao .ts explicita nos imports — algo que o tsc rejeita aqui.
    "**/*.test.ts"
  ]
}
